import PlaceholderPage from "@/src/components/dashboard/PlaceholderPage";

export default function AdminSendNotificationPage() {
  return <PlaceholderPage title="Send Notification" description="Send targeted platform notifications to users, owners, or admins." />;
}
